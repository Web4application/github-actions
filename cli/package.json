{
  "name": "github-pages-cli",
  "version": "1.0.0",
  "type": "commonjs",
  "description": "CLI to scaffold demo GitHub Pages Vue apps",
  "bin": {
    "create-demo-vue": "dist/index.js"
  },
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "ncp": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

"scripts": {
  "lint:check": "eslint .",
  "test": "jest",
  "coverage-badge": "coverage-badge -o coverage/badge.svg -p 85",
  "prepare": "ncc build src/index.ts -o dist"
}
